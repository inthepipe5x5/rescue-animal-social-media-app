/services folder:

    Contains the business logic of your application.
    Handles integration with external services (like PetFinder API).
    Implements core functionality that isn't directly tied to HTTP requests/responses.
